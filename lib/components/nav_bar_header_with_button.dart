import 'package:flutter/material.dart';

class HeaderWidgetWithButton extends StatelessWidget {
  const HeaderWidgetWithButton({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}